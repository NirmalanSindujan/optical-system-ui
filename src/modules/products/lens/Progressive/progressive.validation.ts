export {
  bifocalEditFormDefaultValues as progressiveEditFormDefaultValues,
  bifocalEditFormSchema as progressiveEditFormSchema,
  bifocalFormDefaultValues as progressiveFormDefaultValues,
  bifocalFormSchema as progressiveFormSchema,
  buildBifocalPayload as buildProgressivePayload,
  buildBifocalUpdatePayload as buildProgressiveUpdatePayload,
} from "@/modules/products/lens/Bifocal/bifocal.validation";

export type {
  BifocalEditFormValues as ProgressiveEditFormValues,
  BifocalFormValues as ProgressiveFormValues,
} from "@/modules/products/lens/Bifocal/bifocal.validation";
